import 'package:flutter/material.dart';

// File: likes_page.dart

class LikesPage extends StatelessWidget {
  const LikesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('likes_page'),
      ),
    );
  }
}