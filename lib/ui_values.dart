import 'package:flutter/material.dart';

//color
const mainColor = Color(0xFF00AED7);
const textColor = Color(0xFF01606D);
const bannerColor = Color.fromARGB(255, 113, 210, 231);
const red = Colors.red;
const white  = Colors.white;
const black = Colors.black;


//size
double getSizeHeight(BuildContext context) => MediaQuery.of(context).size.height;
double getSizeWidth (BuildContext context) => MediaQuery.of(context).size.width;
